# Xcode build artifacts
build/
build-device/
build-sim/
build-test/
DerivedData/
*.xcuserstate
xcuserdata/

# Generated by `xcodegen generate` from project.yml — project.yml is the
# single source of truth. Run xcodegen after cloning to open the project.
Estormi.xcodeproj/

# Generated by `make ios-testflight` from APPLE_TEAM_ID + IOS_PROVISION_PROFILE —
# keeps the Team ID + profile name out of committed source.
ExportOptions.plist
