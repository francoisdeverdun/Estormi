# Rust third-party licenses

The Estormi macOS shell (`apps/estormi-macos/`) statically links the Rust crates
listed below into the distributed binary. Their verbatim license and copyright
notices are reproduced here to satisfy the MIT / Apache-2.0 (and similar)
redistribution terms.

**Do not edit by hand** — this file is generated by `make rust-licenses`
(`cargo about generate`) from `Cargo.lock`. Regenerate it after any dependency
change and commit the result.

{{#each licenses}}
## {{name}}{{#if id}} ({{id}}){{/if}}

Applies to:
{{#each used_by}}
- {{crate.name}} {{crate.version}}
{{/each}}

```
{{{text}}}
```

{{/each}}
